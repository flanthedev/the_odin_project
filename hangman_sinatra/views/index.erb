<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
</head>

  <body>
  <div class="text-center">

    <h1>hangman</h1>
    <br />
    <div class="well">

      <h3><%= @message %></h3>
        <% if @lose || @win %>
          <form action="/" method="get">
            <input type="submit" value="yes" class="btn btn-primary">
          </form>
        <% end %>


    <% if !(@lose || @win) %>
      <h3><%= @hint.split(//).join("  ") %></h3>
      <br />

        <p>letters guessed:</p>
        <p><strong><%= @guessed.join(" ") %></strong></p>
        <br />
        <% if 6 - @wrong_guesses > 1 %>
          <p>you have <strong><%= 6 - @wrong_guesses %></strong> guesses left</p>
        <% else %>
          <p>you have <strong><%= 6 - @wrong_guesses %></strong> guess left</p>
        <% end %>


        <p>
          <form action="/" method="post">
            <input type="text" name="guess" size="2" />
            <input type="submit" value="guess"class="btn btn-primary btn-sm" />
          </form>
        </p>
      <% end %>
    </div>
    <p>(The word is <%= @word %>)</p>
  </div>

  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous"></script>
  </body>
</html>
